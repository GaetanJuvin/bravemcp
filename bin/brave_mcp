#!/bin/bash
# bin/brave_mcp

# Change to script directory
cd "$(dirname "$0")/.."

# Run with bundler to ensure gems are found
exec bundle exec ruby -Ilib -e "require 'brave_mcp'; BraveMcp::Server.run"
